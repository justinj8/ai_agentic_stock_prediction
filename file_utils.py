# file_utils.py
def read_summary_file(symbol, filename):
    print(f"[Stub] read_summary_file({symbol!r}, {filename!r}) called")
    # Return dummy text
    return f"Dummy summary for {symbol} from {filename}"